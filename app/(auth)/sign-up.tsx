/* eslint-disable prettier/prettier */
import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

const SignUp = () => {
  return (
    <SafeAreaView>
      <Text>SignUp RRR</Text>
    </SafeAreaView>
  );
};

export default SignUp;
